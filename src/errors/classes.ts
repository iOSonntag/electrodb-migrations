// Concrete error subclasses. Populated in Plan 01-02 Task 2.
// The source-scan invariant in tests/unit/errors/codes.test.ts requires that
// no inline 'EDB_*' literal ever appear in this file — every code MUST be
// imported from './codes.ts'.
export {};
